/* Recursion is required a return parameter,process has to be with outside variant  
 * Recursion finalize with a portal size 
 * Tryarc _o|
 *   With return
 *   Outside digit variant
 * Thread |o| Without return,Parameter can refer value back object.?veritcally
 *  Outside digit variant
 *     no object parameter
 *     
 *  2 a object parament
 * Anothers:
 * 1 Fibonacci Reverse 
 * 2 Rect Fill
* Kolor
 */
 public class ayric_cursive
{
    public int factorial(int size)
    {
        if (size >255)
        {
            return size; 
        }
        //global object variant add remove set
        return factorial(size-1); //? --size difference regioster
    }
}
class ayric_fusion
{

}