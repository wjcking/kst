using System;
using System.Collections.Generic;

class cyber_fonc
{

    public static int main_link = 1;  
    const short
            white_1 = 0,
            white_a = 1,
            white_b = 2;
    public static int Main(string[] lins)
    {
        string A = lins[0], E = lins[1], C = lins[2], F = lins[3], D = "D";

        var kind = (A == E) ? A : E;
            var kande = (E == C) ? E : C;
            var kandie = (E == C) ? A : C;
            var kandy = (E == C) ? C : A;
        var fend = (E == C) ? D : F; //Felanc,Txy,Metrix

        return main_link;
    }
    //Ayrike(MainHastwo)Erika
    public static int Arike(string[] lins)
    {
        if ("Arike" == "Erika") main_link = 55;      
        else if(lins[white_1] == "A") main_link = 11123;          
        else if (lins[1] == "AB") main_link = 21123;
        else if (lins[1] == "AC")main_link = 31123;
        else if (lins[1] == "D")main_link = 4;
        else if (lins[1] == "F") main_link = 1;
        else
            main_link = 0;
       return main_link;
    }

    public short fonction1(short index, int kaoyet, short kaler, int evaluate, long stock, long universal, long statistic)
    {
        switch (index)
        {
            case 1: break;
            case 2: break;
            case 3: break;
            default: break;
        }
        return index;
    }
}
